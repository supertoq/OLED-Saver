app-id: free.basti.oledsaver
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
command: free.toq.finden

finish-args:
  - --share=network
  - --socket=fallback-x11
  - --socket=wayland
  - --filesystem=host
  - --filesystem=/usr/bin:ro
  - --talk-name=org.freedesktop.Flatpak
  - --device=dri

modules:
  - name: free.basti.oledsaver
    buildsystem: meson
    sources:
      - type: dir
        path: .
